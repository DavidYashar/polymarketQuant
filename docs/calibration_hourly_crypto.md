# Calibration report (placeholder)

This file is overwritten by `python/calibration_hourly_crypto.py`.

Run on the old Ubuntu laptop:

```bash
cd ~/polymarketBot/python
source .venv/bin/activate
python calibration_hourly_crypto.py --days 14
```

The first real run will replace this content with the actual calibration tables.
